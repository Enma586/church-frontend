/**
 * Address / geography domain types.
 * Re-exports from the global types module so feature code
 * only imports from its own domain barrel.
 */
export type {
  Department,
  Municipality,
} from '@/types';