/**
 * Journal (Asientos Contables) domain types.
 * Re-exports from the global types module.
 */
export type {
  JournalStatus,
  JournalLine,
  JournalEntry,
  CreateJournalEntryPayload,
  UpdateJournalEntryPayload,
  JournalEntryQueryParams,
} from '@/types';