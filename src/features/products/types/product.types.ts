/**
 * Products (Productos y Servicios) domain types.
 * Re-exports from the global types module.
 */
export type {
  Product,
  CreateProductPayload,
  UpdateProductPayload,
  ProductQueryParams,
} from '@/types';